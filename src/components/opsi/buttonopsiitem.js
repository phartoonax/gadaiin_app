import { Button } from "@mui/material";
import React from "react";

const ButtonOpsiItem = ({ title }) => {
  return (
    <>
      <Button>{title}</Button>
    </>
  );
};

export default ButtonOpsiItem;
